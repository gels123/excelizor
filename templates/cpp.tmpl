/*
 * config class generated by excelizor tool.
 */
#include "../rapidjson/document.h"
#include "../rapidjson/istreamwrapper.h"
#include "../jsonconfig.h"
#include <stdio.h>
#include <cstdio>
#include <string>
#include <fstream>
#include <iostream>

using namespace std;
using namespace rapidjson;
{{- $reflect := "" -}}{{- $tp := ""}}
namespace Configs
{
    class {{.Name}} : public JsonConfig
    {
    public:
        {{- with .OwnTemplate -}}
        {{- range .Fields}}
        {{$tp = GetCppTypeName .LongType}}{{$tp}} {{.Name}};
        {{$reflect = GetCppCtor $reflect $tp .Name}}
        {{- end}}
        {{- end}}
    public:
        {{.Name}}()
        {
            {{$reflect}}
        }
        ~{{.Name}}() {}
    };

{{ "}" -}}//end namespace Configs